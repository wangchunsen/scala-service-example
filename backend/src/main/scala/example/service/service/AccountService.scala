//package example.service.service
//
//import example.service.db.DB
//import example.service.repo.UserRepo
//
//import scala.concurrent.Future
//
//class AccountService(userRepo: UserRepo) {
//
////  import db._
////  import api._
//
////  def accountIdByNameAndPassword(passport: String, password: String): Future[Option[Long]] = run(
////    tables.Account
////      .filter(t => t.passport === passport && t.password === password && t.enabled === true)
////      .take(1)
////      .map(_.id)
////      .result
////      .headOption
////  )
//}
